# Recommendations (2025-11-24 15:53:03)
- Support `reasoning` and `redacted-reasoning` assistant message parts when converting prompts so that upstream APIs receive richer contextual cues.
- Add coverage for proxy/base URL behavior on completion endpoints to mirror the new chat test and ensure parity across modes.
- Consider exposing a typed configuration helper for setting headers (e.g., organization or project IDs) to reduce repetition when integrating with multiple gateways.
